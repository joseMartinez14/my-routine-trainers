export type TrainerProfileForm = {
  name: string;
  phone: string;
  photoURL?: string;
  aboutMe?: string;
};

export type TrainerType = {
  id: string;
  email: string;
  name: string;
  phone: string;
  photoURL?: string;
  aboutMe?: string;
};
